events {
    worker_connections 1024;
}

http {
    upstream backend {
        # Por defecto apunta a Blue (versión estática)
        server blue-app:3000;
        
        # Para cambiar a Green, comentar línea anterior y descomentar:
        # server green-app:3000;
    }

    server {
        listen 80;
        
        # Configuración general para Next.js
        location / {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            
            # Headers adicionales para Next.js
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_cache_bypass $http_upgrade;
            
            # Timeouts
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        

        # Favicon y otros assets
        location ~* \.(ico|css|js|gif|jpe?g|png|svg|woff2?|ttf|eot)$ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            
            # Cache para assets
            expires 30d;
            add_header Cache-Control "public";
        }
    }
}
